---

- name: "Playbook: Upload Scripts to Zabbix Server"
  hosts: "{{ args_host }}"
  gather_facts: false

  tasks:
    - ansible.builtin.include_role:
        name: alex.zabbix.uploadscripts
